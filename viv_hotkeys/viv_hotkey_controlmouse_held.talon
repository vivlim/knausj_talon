tag: user.vivlim_hotkey_controlmouse_held
-
settings():
    # no pop click when i'm using the keyboard, it happens when i just press keys because the keyboard is loud
    user.mouse_enable_pop_click = 0 